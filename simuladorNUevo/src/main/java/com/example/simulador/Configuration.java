package com.example.simulador;

public class Configuration {
    private static int rows = 12; // Valor por defecto
    private static int cols = 12; // Valor por defecto

    public static int getRows() {
        return rows;
    }

    public static void setRows(int rows) {
        Configuration.rows = rows;
    }

    public static int getCols() {
        return cols;
    }

    public static void setCols(int cols) {
        Configuration.cols = cols;
    }

    // Constructor y otros métodos de la clase Configuration

    // Método setter para la propiedad pozo
    public void setPozo(int pozo) {
    }

    // Constructor y otros métodos de la clase Configuration

    // Método setter para la propiedad pozo
    public void setAgua(int agua) {
    }

    // Constructor y otros métodos de la clase Configuration

    // Método setter para la propiedad pozo
    public void setBiblioteca(int biblioteca) {
    }

    // Constructor y otros métodos de la clase Configuration

    // Método setter para la propiedad pozo
    public void setTesoro(int tesoro) {
    }

    // Constructor y otros métodos de la clase Configuration

    // Método setter para la propiedad pozo
    public void setMontaña(int montaña) {
    }

    // Constructor y otros métodos de la clase Configuration

    // Método setter para la propiedad pozo
    public void setComida(int comida) {
    }
}